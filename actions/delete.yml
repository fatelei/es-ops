actions:
  1:
    action: delete_indices
    description: remove indices
    options:
      timeout_override: 300
      ignore_empty_list: True
      disable_action: True
    filters:
    - filtertype: pattern
      kind: prefix
      value: logstash-
